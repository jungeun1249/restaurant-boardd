<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>댓글 수정</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container mt-5">
    <h4>✏️ 댓글 수정</h4>
    <form action="/comment/<%= comment.id %>" method="POST">
      <textarea name="content" class="form-control mb-3" rows="4" required><%= comment.content %></textarea>
      <button class="btn btn-primary">수정 완료</button>
      <a href="/post/<%= comment.postId %>" class="btn btn-secondary">취소</a>
    </form>
  </div>
</body>
</html>
