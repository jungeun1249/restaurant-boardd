<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>맛집 게시판</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background:#f8f9fa; padding:40px; font-family:'Pretendard'; }
    .container { max-width:1000px; }
    h2 { font-weight:700; margin-bottom:30px; display:flex; align-items:center; justify-content:space-between; }
    a { text-decoration:none; color:#5a189a; }
    a:hover { text-decoration:underline; }
    .rating { color:#ffb703; font-size:18px; }
    .btn-primary { background:#5a189a; border:none; }
    .btn-primary:hover { background:#7b2cbf; }
  </style>
</head>
<body>
  <div class="container">
    <h2>
      🍽️ 맛집 게시판
      <div>
        <a href="/profile" class="btn btn-outline-dark btn-sm me-2">👤 내 정보</a>
        <a href="/logout" class="btn btn-outline-secondary btn-sm">🚪 로그아웃</a>
      </div>
    </h2>

    <form method="get" class="row g-2 mb-3">
      <div class="col-md-6">
        <input type="text" class="form-control" name="q" value="<%= query || '' %>" placeholder="제목 검색">
      </div>
      <div class="col-md-2">
        <button class="btn btn-primary w-100">검색</button>
      </div>
      <div class="col-md-2 ms-auto text-end">
        <a href="/write" class="btn btn-success w-100">✏️ 글쓰기</a>
      </div>
    </form>

    <table class="table table-hover table-bordered bg-white shadow-sm">
      <thead class="table-light text-center">
        <tr>
          <th style="width:40%">
            <a href="/board?sort=title&order=<%= sort==='title' && order==='asc' ? 'desc':'asc' %>">제목</a>
          </th>
          <th style="width:15%">
            <a href="/board?sort=rating&order=<%= sort==='rating' && order==='asc' ? 'desc':'asc' %>">평점</a>
          </th>
          <th style="width:15%">작성자</th>
          <th style="width:30%">
            <a href="/board?sort=date&order=<%= sort==='date' && order==='asc' ? 'desc':'asc' %>">작성일</a>
          </th>
        </tr>
      </thead>
      <tbody>
        <% if (posts.length === 0) { %>
          <tr><td colspan="4" class="text-center text-muted py-4">작성된 글이 없습니다 😢</td></tr>
        <% } %>
        <% posts.forEach(post => { %>
          <tr>
            <td><a href="/post/<%= post.id %>" class="fw-semibold"><%= post.title %></a></td>
            <td class="text-center rating">
              <%= '★'.repeat(post.rating || 0) %><%= '☆'.repeat(5 - (post.rating || 0)) %>
            </td>
            <td class="text-center"><%= post.username %></td>
            <td class="text-center text-secondary"><%= new Date(post.createdAt).toLocaleString('ko-KR') %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</body>
</html>
