<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= title || '게시판' %></title>
  
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;700&display=swap" rel="stylesheet">
  
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <style>
    body {
      font-family: 'Noto Sans KR', sans-serif;
      font-size: 18px; 
    }
    .navbar-nav .nav-link {
        transition: color 0.3s ease;
    }
    .navbar-nav .nav-link:hover {
        color: #ffc107 !important;
    }
  </style>

  <script src="//dapi.kakao.com/v2/maps/sdk.js?appkey=d167bce40f724f4556f19ac97ffbd95d"></script>
</head>

<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container">
      <a class="navbar-brand" href="/">맛집 게시판</a>
      <div class="collapse navbar-collapse">
        <ul class="navbar-nav ms-auto">
          <% if (session?.username) { %>
            <% if (session.profileImage) { %>
              <li class="nav-item">
                <a class="nav-link p-0" href="/profile">
                  <img
                    src="/uploads/<%= session.profileImage %>"
                    alt="프로필"
                    class="rounded-circle"
                    style="width:32px; height:32px; object-fit:cover; margin-right:8px;"
                  />
                </a>
              </li>
            <% } %>
            <li class="nav-item">
              <a class="nav-link" href="/profile">👤 내 정보</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/logout">로그아웃</a>
            </li>
          <% } else { %>
            <li class="nav-item">
              <a class="nav-link" href="/login">로그인</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/register">회원가입</a>
            </li>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container">
    <%- body %>
  </div>
</body>
</html>